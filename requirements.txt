fastapi==0.109.0
uvicorn==0.24.0
python-dotenv==1.0.0
pillow>=11.0.0
pydantic>=2.4.0
numpy<2
requests>=2.31.0
pytest>=8.0.0

# Optional for SAM segmentation. Install manually with a compatible torch build:
# pip install torch torchvision
# pip install git+https://github.com/facebookresearch/segment-anything.git
# Optional SAM integration (disabled for serverless deploys)
# If you need SAM locally or on a suitable VM, install manually:
# pip install git+https://github.com/facebookresearch/segment-anything.git
